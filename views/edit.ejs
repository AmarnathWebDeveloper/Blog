<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Post</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/style.css" />
  </head>

  <body>
    <div class="flex-container">
      <h1>Blog</h1>
      <form action="/editPostData" method="post">
        <div class="grid-container">
          <input type="text" name="title" value="<%=editData["title"]%>"
          required/> <input type="text" name="author"
          value="<%=editData["author"]%>" required/>
          <textarea name="content" id="_content" cols="30" rows="18" required><%= editData["content"] %></textarea>
          <input type="hidden" name="index" value="<%=Index%>" />
          <%let date = new Date().getDate()
          let month = new Date().getMonth()+1
          let year = new Date().getFullYear()
          let hour = new Date().getHours()
          let min = new Date().getMinutes()
          if(date<10){date="0"+date}
          if(month<10){month="0"+month}
          if(hour<10){month="0"+hour}
          if(min<10){min="0"+min}
          let dayMonth = date+"/"+month+"/"+year+" | "+hour+":"+min%>
        <input type="hidden" name="time_date" value="<%=dayMonth%>">
        </div>
        <div class="flex-container-menu">
          <button type="submit">Edit & Post</button>
        </div>
      </form>
    </div>
    <div class="flex-container-menu">
      <a href="/back" id="_back"><button>Back</button></a>
    </div>
  </body>
</html>
